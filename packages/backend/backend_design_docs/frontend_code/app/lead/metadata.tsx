import type { Metadata, Viewport } from 'next'

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  themeColor: '#1e40af',
}

export const metadata: Metadata = {
  title: 'Lead Dashboard | Miliare',
  description: 'Dashboard for lead users to manage their assigned accounts.',
}
